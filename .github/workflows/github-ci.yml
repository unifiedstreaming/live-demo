name: Docker Compose Actions Workflow
on:
  push:
    branches:
      - ci-test
jobs:
  build-ffmpeg:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker-compose build --no-cache ffmpeg
